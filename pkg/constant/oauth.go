package constant

// OAuth providers
const (
	GOOGLE_OAUTH_PROVIDER    = "google"
	FACEBOOK_OAUTH_PROVIDER  = "facebook"
	MICROSOFT_OAUTH_PROVIDER = "microsoftonline"
)

// OAuth cache key prefix
const (
	SESSION_KEY_OAUTH_CACHE = "oauth-session"
	TOKEN_KEY_OAUTH_CACHE   = "oauth-token"
)
